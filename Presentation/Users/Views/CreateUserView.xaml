<UserControl x:Class="WpfAppTest.Presentation.Users.Views.CreateUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create a new user"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0 0 0 16"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 16">
            <TextBlock Text="Name" FontWeight="SemiBold" Margin="0 0 0 6"/>
            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 16">
            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0 0 0 6"/>
            <TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="3">
            <Button Content="Create User"
                    Width="160"
                    HorizontalAlignment="Left"
                    Command="{Binding CreateUserCommand}"/>
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextMutedBrush}"
                       Margin="0,12,0,0"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
